# RELEASE NOTES — SAKURA 請求書システム

## v1.6.0 — 2026-04-13

### 改善：メール本文をモード別・新フォーマットに更新

- 請求書 / 見積書 / 納品書 の各モードで、メール件名・本文を自動切り替えするよう変更。
- 請求書モード：支払い方法（クレジット15日・口座振替22日）・Stripe注記・HP URL を丁寧な文体で追加。
- 見積書モード：「お見積金額」表記・HP URL を含む簡潔なフォーマット（支払い情報なし）。
- 納品書モード：金額省略・HP URL を含む簡潔なフォーマット（支払い情報なし）。
- 区切り線を `――――――――――――――――――` 形式に統一、署名欄の全角コロン・スペースを整形。

## v1.5.9 — 2026-04-10

### 機能追加：メール宛先を複数人に送信可能に

- 宛先欄をカンマ区切りで複数メールアドレスを入力できるように変更（例: `aaa@bbb.com,xxx@zzz.com`）。
- JS側で各アドレスを個別にバリデーション。
- PHP側でカンマ区切りを解析し、`mb_send_mail` に渡す宛先文字列を生成。
- PDF添付ありなし両方の送信フローに対応。

---

## v1.5.8 — 2026-04-10

### バグ修正：D&Dで複数ファイルを累積追加できるよう修正

- ドロップ・クリック選択のたびにファイルを**累積追加**する方式に変更（以前はドロップのたびに上書き）。
- 同名ファイルの重複追加を防止。
- 各ファイルに ✕ ボタンを追加し、個別削除が可能に。
- PDF以外のファイルはドロップ時に無視。

---

## v1.5.7 — 2026-04-10

### 機能改善：PDF添付欄をドラッグ＆ドロップ対応に変更

- 添付ファイル選択欄をドロップゾーンUIに変更。PDFをドラッグ＆ドロップで追加可能。
- クリックしてファイル選択する従来の操作も引き続き可能。
- ドロップ後・選択後にファイル名を番号付きバッジで一覧表示。
- モーダルを閉じると選択状態とドロップゾーンの表示をリセット。

---

## v1.5.6 — 2026-04-10

### 機能追加：メール送信で複数PDF添付に対応

- **`invoice.html`**: 添付ファイル選択欄に `multiple` 属性を追加。複数PDFを一度に選択可能に。
- **`invoice.html`**: 選択したファイル名を番号付きで一覧表示する `updateMailPdfList()` を追加。
- **`invoice_send.php`**: `pdfs: [{pdf_base64, pdf_filename}, ...]` の配列形式に対応し、複数ファイルをMIMEマルチパートで添付して送信。
- PDF無し送信（`sendMail()`）の動作は変更なし。

---

## v1.5.5 — 2026-04-06

### バグ修正：顧客選択時に請求書一覧が即座に表示されない問題を解消

- **フィルタリング漏れの修正**:
    - `openCustomerInvoices`, `newInvoice`, `loadInvoice` 等の内部関数において、正規化（`_normalize`）が適用されていなかった箇所をすべて修正。
    - これにより、顧客カードをクリックした直後（保存ボタンを押す前）から、左メニューに過去の請求書が即座に表示されるようになりました。

---

## v1.5.4 — 2026-04-06

### 機能改善：検索正規化の導入と顧客マスター連動更新

- **検索ロジックの正規化（最強の正規化）**:
    - `_normalize()` 関数を導入し、全角・半角・大文字・小文字・空白の有無を問わず検索・フィルタリングができるように改善。
    - これにより「Daiko株式会社」と「Ｄａｉｋｏ　株式会社」などが同一視され、検索漏れや表示不良が解消されました。
    - 影響範囲：顧客一覧検索、請求書一覧検索、サイドバーの顧客別フィルタ、顧客カード内の請求書件数カウント。

- **顧客マスター（customer_records）への自動反映**:
    - 請求書を保存する際、入力されたメールアドレス・電話番号・住所がマスターデータと異なる場合、マスター側も自動的に更新する機能を追加。
    - これにより、一度請求書で情報を修正して保存すれば、次回その顧客で新規請求書を作成する際にも最新の情報が自動で反映されます。

---

## v1.5.3 — 2026-04-06

### バグ修正：見積書・納品書の弊社情報・印鑑表示

- 見積書・納品書モードのPDF右上の会社名が `SAKURA-NET` になっていた問題を修正
- 正しく `株式会社さくらねっと` が表示されるよう修正（`getCompanyName()` の条件を修正）
- 見積書・納品書モードで会社印が表示されなかった問題を修正（請求書モードと同じく表示されるよう修正）
- `normal`（通常請求書）モードのみ従来通り `SAKURA-NET` / 会社印非表示を維持

---

## v1.5.2 — 2026-03-25

### セキュリティ強化：Firestore クライアント アクセスをブロック

**背景:**
- Firebase テストモードのセキュリティルールが30日期限で切れる予定
- クライアント側の Firestore 直接アクセスは セキュリティリスク

**実装:**
1. **Firestore セキュリティルール設定** - すべてのクライアントアクセスを拒否（`allow read, write: if false;`）
2. **invoice.html 修正** - `subscribeInvoices()` と `loadCustomerCache()` を無効化
3. **動作モード** - ローカルストレージのみで動作（過去の請求書データは読み込まれません）
4. **メール送信** - サーバーサイド（PHP）での処理は継続

**注意:**
- 既存の Firestore データは保持されます（削除されません）
- 新規作成した請求書はローカルストレージに保存
- メール送信時に履歴が必要な場合は、今後 Admin SDK の導入を検討

### 機能追加：メール送信時に CC を自動追加

- メール送信時に `system@sakuranet-co.jp` を **Cc** ヘッダーに自動追加
- メールソフト（Thunderbird等）の送信済みフォルダにメール送信履歴を保存可能に
- IMAP接続時に送信済みアイテムとして自動同期される
- 受信者に CC が表示される（透明性向上）

---

## v1.5.1 — 2026-03-23

### 新機能：品目オートコンプリート

- 品名入力欄にフォーカスまたは入力すると候補ドロップダウンを表示
- 候補は全顧客の過去請求書データから自動生成（登録作業不要）
- 候補選択で品名・単位・単価を同時に自動入力
- ↑↓キーで候補移動、Enterで確定、Escで閉じる
- 請求書保存時・Firestore読込時に候補データを自動更新

---

## v1.5.0 — 2026-03-23

### 新機能：まとめ請求書PDF出力機能を追加

- 顧客グリッドの各カードにチェックボックスを追加
- 2件以上チェックすると「まとめてPDF（N件）」バーが上部に表示される
- 「📄 PDF生成」ボタンで各顧客の最新請求書を1つのA4マルチページPDFに出力
- 同一会社名プレフィックスの顧客には「🏢 まとめて選択（N件）」ボタンを表示し一括選択可能
- 異なる会社を混在させようとした場合に確認ダイアログを表示
- PDF送信先はチェック順1番目の顧客のメールアドレスを使用
- 「✕ キャンセル」ボタンで全チェックを解除

---

## v1.4.3 — 2026-03-23

### バグ修正：顧客グリッドの重複表示を修正

- 全角/半角スペースの違いで同一顧客が複数表示される問題を修正
- `renderCustomerGrid()` に normName 重複除去を追加（グループウェアと同じロジック）

---

## v1.4.2 — 2026-03-23

### 新機能：顧客カード個別非表示機能を追加

- 各顧客カードにホバーで「🙈 非表示」ボタンが表示されるように
- 押すと該当顧客が一覧から非表示になる（localStorage に保存・リロード後も維持）
- 非表示の顧客がいる場合、ヘッダーに「👁 非表示：N件」ボタンが表示される
- そのボタンを押すと非表示顧客のみ一覧表示され、「✅ 再表示」で元に戻せる

---

## v1.4.1 — 2026-03-23

### バグ修正：顧客名サジェストの重複表示を修正

- 全角スペース・半角スペースの表記ゆれにより「株式会社やすら木　寺方」など同一顧客が複数表示される問題を修正
- `normName()` 関数を追加し、全角スペースを半角に統一・連続スペースを1つに圧縮してからMap重複排除キーに使用するよう変更
- `customerCache`（顧客マスタ）・`invoiceCache`（既存請求書）の両方で正規化を適用

---

## v1.4.0 — 2026-03-21

### 書類種別モード切替機能の追加

- **4種のモードをヘッダーセレクトで切替可能に**: インボイス対応請求書 / 見積書 / 納品書 / 通常請求書（SAKURA-NET）
- **見積書モード**: タイトルを「見積書」に変更、登録番号・消費税テキストを非表示
- **納品書モード**: タイトルを「納品書」に変更、登録番号・消費税テキストを非表示
- **通常請求書モード（SAKURA-NET）**: 会社名をSAKURA-NETに変更、登録番号非表示、会社印非表示
- **モード設定はlocalStorageで保持**: ページリロード後も選択状態を維持
- **メール件名・本文・PDFファイル名**: 選択モードに応じて自動切替

---

## v1.3.7 — 2026-03-21

### PDF品質問題の根本解決と送信フローの最適化

- **手動添付方式への移行**: レンダリングエンジンの限界による「紫色背景」や「改ページ問題」を完全に排除するため、ブラウザ標準の高品質な印刷PDFを手動で添付して送信する方式を採用しました。
- **ファイル名の動的生成**: ブラウザ印刷時に `document.title` を動的に書き換えることで、保存時のデフォルトファイル名を「○月分請求書（○様分）」という管理しやすい形式に自動設定する機能を実装。
- **メール送信モーダルの改善**: ファイル選択欄（📎 添付PDF）を追加し、保存したPDFをその場で選択して安全に送信できるUIへアップグレード。
- **バリデーションの強化**: PDFの選択忘れを防ぐチェックロジックを追加。

---

## v1.3.6 — 2026-03-21

### v1.2.7 安定版への完全リバートと機能復旧

- **完全リバート**: ユーザー要望に基づき、システムを「ほぼ完璧」と評価された `v1.2.7` の機能ロジックに差し戻しました。
- **PDF生成の安定化**: `html2pdf.js` の設定を `v1.2.7` 当時の構成（scale: 3, margin: [15, 0, 15, 0]）に復元し、メール送信時の実行エラーを解消。
- **構造の復元**: A4キャンバスを `min-height: 297mm` のシンプルな `div` 構造に戻し、レンダリングの互換性を確保。
- **デザインの維持**: 機能面はリバートしつつ、ユーザーが不要とした「紫色」の装飾を排除したモノトーンデザインは継続して適用。

---

## v1.3.5 — 2026-03-21

### メール送信機能の完全復旧と構造の抜本的リセット

- **システムリセット**: 度重なるレンダリング調整により複雑化していた内部構造とオプション設定を、最も動作が安定していた初期状態へとリセットしました。
- **構造の簡素化**: キャプチャ対象である `a4Canvas` の外部ラッパーを削除し、DOMツリーの干渉による実行時エラー（メール送信不能問題）を解消。
- **オプションの最小化**: `html2pdf.js` の設定を最小構成（scale: 2, margin: 0, 外部フックなし）に戻し、実行パフォーマンスと互換性を最優先に復旧。

---

## v1.3.4 — 2026-03-21

### メール送信機能の復旧とデザインの最終統一

- **機能復旧**: `v1.3.2` で導入したレンダリング範囲制限（width/height）が一部の環境で動作を停止させていたため、これを除去し、メール送信機能の安定性を `v1.3.1` 相当に復旧。
- **デザインの無彩色化（再徹底）**: 請求書タイトル、品目テーブル、フッター、備考欄から残存していた紫色・ピンク色を完全に排除。ビジネス利用に最適なモノトーン基調のデザインへ完全移行。
- **構造のクリーンアップ**: キャプチャ対象外の不要なスタイル属性を削除し、レンダリング負荷を軽減。

---

## v1.3.3 — 2026-03-21

### 請求書デザインのプロフェッショナル化（無彩色化）

- **紫色・ピンク色の完全排除**: 請求書内の装飾バー、タイトルアクセント、テーブルヘッダーのグラデーション、およびフッターの合計欄から紫色とピンク色を排除し、黒・グレー基調のクリーンなデザインへ変更。
- **視認性の向上**: テーブルの縞模様を極薄のグレーに変更し、ビジネス文書としての品位と読みやすさを両立。
- **1ページ出力の維持**: 不要な装飾（6pxのヘッダーバー等）を削除したことで、垂直方向の余裕がさらに拡大し、20行表示時の安定性が向上。

---

## v1.3.2 — 2026-03-21

### PDF送信時の「空白の2ページ目」の徹底排除

- **ピクセル単位のクリッピング**: `html2canvas` でのキャプチャ範囲をA4の物理サイズよりわずかに内側（793px x 1120px）で厳密にクリッピングし、微小なはみ出しを強制カット。
- **マージンと改ページの抑制**: `.pi-page` およびそのコンテナに対して `margin: 0` と `break-inside: avoid` を適用し、ブラウザによる自動改ページを物理的に抑制。
- **コンテナスタイルの整理**: `a4Content` に高さ固定と `overflow: hidden` を追加し、内部階層でのサイズ超過を遮断。

---

## v1.3.1 — 2026-03-21

### PDFレンダリング精度の最終改善（安定化）

- **レイアウト崩れの解消**: 描画オプションの `windowWidth/Height` 指定を撤回し、プレビュー画面そのままの比率でキャプチャするように最適化。
- **スタイルの強制適用（onclone）**: PDF生成時のクローン要素に対して、背景色の徹底的な白色化（#ffffff）と影の除去を手動で適用。
- **描画品質の安定化**: 解像度設定を `scale: 2` に固定し、ブラウザによる描画誤差を最小限に。

---

## v1.3.0 — 2026-03-21

### PDF送信時のレンダリング差異の完全修正

- **背景色のホワイト化保証**: PDF生成時に周囲のテーマカラー（紫色など）が混入しないよう、出力キャンバスの背景色を `!important` で白色に強制固定。
- **2ページ目発生の物理的防止**: A4の高さに対して `1mm` の安全マージン（296mm）を設定し、計算誤差による改ページを確実に排除。
- **レンダリング精度の最適化**: `html2canvas` の解像度とキャプチャ範囲をピクセル単位で厳密に定義（794x1123px）し、プレビューとPDFの見た目を一致。

---

## v1.2.9 — 2026-03-21

### PDF送信時のページ数最適化（再調整）

- **キャンバスの純粋化**: PDFキャプチャ対象の要素から `box-shadow` を分離し、1pxのズレも許さない完全なA4サイズ（210x297mm）に調整。
- **垂直方向のサイズ調整**: `.pi-content` の上部余白を `22mm`（3mm削減）、明細行の上下パディングを `4px`（1px削減）に微調整。これにより20行表示時の物理的なオーバーフローをさらに抑制。

---

## v1.2.8 — 2026-03-21

### PDF送信時のページ数最適化

- **空白ページ削除**: PDF生成時に意図せず2ページ目（空白）が作成される問題を解決するため、生成HTMLのトリミングおよび改ページ防止設定（pagebreak avoid）を追加。
- **キャプチャ精度の向上**: スクロール位置の影響を排除する設定を追加し、安定したPDF出力を実現。

---

## v1.2.7 — 2026-03-21

### PDF出力レイアウトの再最適化

- **上部余白の確保**: `.pi-content` のパディングを `25mm` に設定し、PDF出力時の窮屈さを解消。
- **空白行の整理**: 明細テーブル上部の不要な3行の空白行を削除し、内容が上から始まるように変更。
- **明細行の拡張**: 明細テーブルの最大表示行数を20行に固定し、より多くの項目を表示可能に。
- **PDF出力エンジン調整**: 単位を `mm` に統一し、解像度（scale）を3に向上させてA4印刷品質を最適化。

---

## v1.2.6 — 2026-03-21

### PDF出力レイアウトの最終最適化

- PDF出力時の上部余白を拡大（25mm）
- 明細テーブル上部の不要な3行の空白行を削除
- 明細の最大行数を20行に拡張
- 請求対象月（billingMonth）ラベルの非表示化
- PDF出力エンジン（html2pdf.js）のオプション最適化（単位をmmに統一、解像度向上）

---

## v1.2.5 — 2026-03-21

### PDFレイアウト最適化 (Gemini直接改修)

- **上部余白の拡張**: PDF出力時の窮屈さを解消するため、上部余白を `25mm` に拡張。
- **発行日・No.エリア調整**: 上部余白の変更に合わせて、絶対配置されている発行日エリアの初期位置を `30mm` 下げました。
- **請求月ラベルの削除**: デザインをスッキリさせるため、PDF出力時の請求対象月表示を廃止。
- **PDF生成設定の高度化**: `html2canvas` のスケールを `3` に引き上げ、かつ `jsPDF` の単位を `mm` に変更してA4サイズへの適合性を向上させました。

---

## v1.2.5 — 2026-03-21

### 品目テーブル改善・発行日エリアドラッグ対応

- 品目テーブル先頭に3行の空白行を追加
- 空白行数を12→20行に拡張（合計8行増）
- 請求対象月（billingMonth）表示を廃止
- 発行日・No.エリアをドラッグで位置変更できるように（localStorage保存）

---

## v1.2.4 — 2026-03-21

### v1.2.2/v1.2.3の印刷CSS変更をリバート

- `@page { margin: 25mm }` → `@page { margin: 0 }` に戻す
- `a4Canvas`・`pi-page` を v1.2.1 状態（`width:210mm` / `height:297mm`）に戻す
- v1.2.2/v1.2.3の変更でレイアウトが崩れたため全リバート

---

## v1.2.3 — 2026-03-21

### 印刷時レイアウト崩れ修正（→ v1.2.4でリバート）

- `@media print` で `#a4Canvas` を `width: 100%` / `height: auto` / `overflow: visible` に上書き

---

## v1.2.2 — 2026-03-21

### 印刷余白をWord標準に統一

- `@page margin: 25mm`（上下左右 Word標準）に変更
- 印刷時の `.pi-page` を `width: 160mm / height: auto` に調整
- プリンタメーカーによる余白不足問題を解消

---

## v1.2.1 — 2026-03-21

### PDFレイアウト微調整

- PDFテンプレートから備考（invNote）セクションを削除
- `pi-content` / ヘッダー行 / 顧客情報セクションの余白削減でA4に収まりやすく

---

## v1.2.0 — 2026-03-21

### PDF出力・印刷レイアウト最適化

| ファイル | 種別 | 内容 |
|---------|------|------|
| `invoice.html` | 改修 | A4印刷・PDFレイアウト全面調整 |

1. **A4サイズ物理単位化**
   - `a4Canvas` / `.pi-page` を `794px` → `210mm × 297mm` に変更
   - `overflow: hidden` 追加でコンテンツはみ出し防止

2. **印刷CSSをマニュアル方式に刷新**
   - `@page { size: A4; margin: 0; }` を明示追加
   - 印刷時にプレビューモーダルのヘッダーバー（ボタン類）を非表示化
   - `print-color-adjust: exact` でグラデーション等の色を正確に印刷

3. **品目テーブル列幅調整**
   - 商品名列: 30% → 40%（長い品名に対応）
   - 備考列: 30% → 20%（バランス改善）

4. **概要欄（ヘッダー行）スタイル修正**
   - 太字・紫背景・左ボーダーを削除しシンプルに統一
   - `font-weight: normal` 明示でPDF出力時の太字化を防止

5. **余白・パディング削減**
   - `.pi-content` padding: `28px 56px 48px` → `20px 40px 24px`
   - `.pi-table td` padding: `7px 8px` → `5px 6px`
   - 空白行数: 20行 → 12行（ページ下部の余白過多を解消）

6. **商品名・備考の動的フォントサイズ廃止**
   - 文字数に応じた可変フォントサイズを廃止し固定に統一（デザイン一貫性確保）

---

## v1.1.0 — 2026-03-19

### 機能追加・改善

| ファイル | 種別 | 内容 |
|---------|------|------|
| `invoice.html` | 改修 | A4プレビュー・印鑑ドラッグ・PDF生成・メール添付フロー |
| `invoice_send.php` | 改修 | PDF添付（multipart/mixed MIME）対応 |

1. **請求対象月フィールド追加**
   - 支払期限フィールドを廃止し「請求対象月」年月ドロップダウンに変更
   - デフォルト: 発行日が15日以前→前月、16日以降→当月
   - Firestore: `invDueDate` 廃止 → `billingMonth`（例: `"2026-03"`）追加

2. **A4プレビューモーダル**
   - 「🖨 PDF出力」クリックでA4レイアウトのプレビューが表示される
   - 794px × 1123px（A4相当）の固定幅レイアウト

3. **電子印鑑ドラッグ配置**
   - 社印・個人印をプレビュー上でドラッグして自由に配置
   - 配置位置を `localStorage` に保存（次回起動時も維持）

4. **ブラウザPDF生成（html2pdf.js）**
   - html2pdf.js (v0.10.1) でA4 PDFをブラウザ側で生成
   - 印鑑の配置位置を含めたままPDF化

5. **PDF添付メール送信**
   - 「📧 このPDFで送信」でメール確認モーダルを表示
   - 宛先・件名・本文を自動入力
   - PDF base64 → `invoice_send.php` にPOST → multipart/mixed MIMEで添付送信

---

## v1.0.0 — 2026-03-19

### 新規リリース

| ファイル | 種別 | 内容 |
|---------|------|------|
| `invoice.html` | 新規 | 請求書作成・管理・PDF出力メインUI |
| `invoice_send.php` | 新規 | メール送信バックエンド（mb_send_mail） |

### 主な機能

1. **請求書作成**
   - 顧客名・担当者・メールアドレス・住所の入力
   - 品目（品名/数量/単位/単価）の動的追加・削除
   - 消費税（10%）の自動計算
   - 請求書番号の自動採番（INV-YYYYMM-XXX 形式）

2. **複写機能**（弥生の「複写」と同等）
   - 過去の請求書を選択して「複写」ボタンで複製
   - 顧客情報・品目を引き継ぎ、番号・日付だけ新規に更新

3. **Firestore 連携**
   - 請求書データを `sakura-net-db` Firestoreへリアルタイム同期
   - ステータス管理（下書き / 送信済み / 入金済み / 未入金）

4. **PDF出力**
   - ブラウザの印刷機能（Ctrl+P）で請求書PDFを生成
   - A4縦レイアウト専用CSSで印刷最適化

5. **メール送信**
   - `system@sakuranet-co.jp` から宛先へ送信
   - 件名・本文テンプレートを自動生成（編集可能）
   - 送信完了後にFirestoreのステータスを「送信済み」に自動更新

### 設定が必要な項目

- `invoice_send.php` のSMTP設定（サーバー管理画面で確認）
- `invoice.html` 内の会社情報（住所・電話番号）を実際の情報に更新
- 振込先口座情報のデフォルト値を更新
